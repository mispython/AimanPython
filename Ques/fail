def _char_at(s: str, pos1: int) -> str:
    """SAS-style SUBSTR(s, pos, 1): 1-indexed, blank-padded past end."""
    i = pos1 - 1
    return s[i] if i < len(s) else " "




for r in rows:
    itcode = r["ITCODE"]
    if _char_at(itcode, 14) in ("F", "#"):
        continue
    amtind = r["AMTIND"]
    if amtind and amtind != " ":
        if itcode[0:3] == "307":
            sp.append(r)
        elif itcode[0:1] != "5":
            if itcode[0:3] in ("685", "785"):
                sp.append(r)
            else:
                al.append(r)
        else:
            ob.append(r)
    else:
        if itcode[1:2] == "0":        # note: SAS SUBSTR(...,2,1)="0" on short str -> " "
            sp.append(r)






4219960UMA;0;0;0                                                                
4219976PBCARD;8051;0;0                                                          
4219976UMA;0;0;0 
4219985PBCARD;325;0;0                                                           
4219985UMA;0;0;0
