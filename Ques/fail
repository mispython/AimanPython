def _get_full_header(include_bank: bool, day_code: str, rdate: str, sdesc: str = None) -> list[str]:
    """Return only the title lines (bank name, detail title, report date) for a day."""
    return _get_day_title_lines(include_bank, day_code, rdate, sdesc)
