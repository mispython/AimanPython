import polars as pl
from pathlib import Path

f = Path("/sas/python/virt_edw/Data_Warehouse/MIS/XMIS/output/EIBDUNDP/CARD_DPACTV_260923.txt")

# last customer names in your Python output
names = []
with open(f, encoding="latin1") as fh:
    for line in fh:
        if len(line) > 30 and not line.lstrip().startswith("-") and not line.lstrip().startswith("="):
            name = line[1:28].strip()
            if name:
                names.append(name)

print("Last 5 customer names in Python output:")
for n in names[-5:]:
    print(" ", n)




grep -c "^ [A-Z]" /sas/python/virt_edw/Data_Warehouse/MIS/XMIS/output/EIBDUNDP/CARD_DPACTV_260923.txt
grep -c "^ [A-Z]" /stgsrcsys/host/uat/AII/EIBDUNDP/EIBDUNDP_OUTPUT.TXT
