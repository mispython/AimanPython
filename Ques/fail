import pyarrow.parquet as pq

cache_path = "/sas/python/virt_edw/Data_Warehouse/MIS/XMIS/input/cache/EIIMRPTS/enrh_ln_note_d22.parquet"
try:
    # Read just the metadata without loading data
    md = pq.read_metadata(cache_path)
    print(f"✅ Valid Parquet file – rows: {md.num_rows:,}, columns: {md.num_columns}")
except Exception as e:
    print(f"❌ Invalid or incomplete file: {e}")
