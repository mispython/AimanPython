# 2. _fmt_str: blank char fields must render as a single space, not empty,
#    to match SAS's blank-character PUT behavior and preserve column spacing.

def _fmt_str(value) -> str:
    """Format a character field: trim leading/trailing blanks.

    Equivalent of SAS `var +(-1)` trimming the trailing blank that list
    output would otherwise add before the delimiter.
    """
    if value is None:
        return " "
    stripped = str(value).strip()
    return stripped if stripped else " "


# 3. ADMIN (CBOP CODE) is numeric — use _fmt_num, not _fmt_str,
#    in BOTH _build_inward_line and _build_outward_line.

# OLD (in both functions):
# _fmt_str(row["ADMIN"]),

# NEW:
_fmt_num(row["ADMIN"]),
