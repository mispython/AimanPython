# CISLN (~14.2 GB) — fixed filename, no date pattern
if not _cache_is_fresh(CISLN_FILE, CISLN_CACHE):
    sas_to_parquet(CISLN_FILE, CISLN_CACHE, "CISLN")
else:
    print(f"  [CISLN] Cache fresh — skipping conversion.")

# CISDP (~1.2 GB) — fixed filename, no date pattern
if not _cache_is_fresh(CISDP_FILE, CISDP_CACHE):
    sas_to_parquet(CISDP_FILE, CISDP_CACHE, "CISDP")
else:
    print(f"  [CISDP] Cache fresh — skipping conversion.")
