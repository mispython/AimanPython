con = duckdb.connect(database=":memory:")
cols = con.execute(f"DESCRIBE SELECT * FROM read_parquet('{LOAN_CACHE}')").pl()
with pl.Config(tbl_rows=999):
    print(cols.select("column_name", "column_type"))
con.close()
