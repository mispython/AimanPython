import duckdb
con = duckdb.connect(database=":memory:")
print(con.execute("""
    SELECT
        CAST(OPEN_IND AS VARCHAR)               AS OPEN_IND,
        COUNT(*)                                AS N,
        SUM(CASE WHEN CAST(CURR_BAL AS DOUBLE) > 0 THEN 1 ELSE 0 END) AS N_POS_BAL
    FROM read_parquet('/sas/python/virt_edw/Data_Warehouse/MIS/XMIS/input/cache/EIBDLIQP/fd_pbb.parquet')
    GROUP BY 1
    ORDER BY N DESC
""").fetchall())
con.close()
