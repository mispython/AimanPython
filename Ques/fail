if note1_sorted is None or note2_sorted is None:
    raise RuntimeError(f"[{bank_name}] NOTE1/NOTE2 sorting failed before completion")

print(f"[{bank_name}] NOTE1 rows : {len(note1_sorted):,}")
print(f"[{bank_name}] NOTE2 rows : {len(note2_sorted):,}")
