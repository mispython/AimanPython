    cust = '08' if r["CUSTCD"] in (77, 78, 95, 96) else '09'
    matdt = r["MATDT"]
    lastmat = r["LASTMAT"]          # <- new
    if matdt is None:
        continue
    if r["OPENIND"] == 'D' or (matdt - reptdate).days < 8:
        rem = 0.1
    elif lastmat is not None and -8 <= (lastmat - reptdate).days <= 8:
        rem = 0.1                    # <- new: recent last maturity => immediate
    else:
        rem = remmth(matdt)
