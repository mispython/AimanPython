    meta = ovdr.groupby("ACCTNO").agg({
        "BRANCH": "first",
        "LMTBASER": "first",
        "NAME": "first",
        "ODSTATUS": "first",
        "APPRLIMT": "first"
    }).reset_index()

    odmerg = odmerg.merge(meta, on="ACCTNO", how="left")

    return odmerg
