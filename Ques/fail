    # ADD THIS DEBUG BLOCK AFTER the for loop
    print(f"\n--- MAPPED SET_IDs ({len(mapped)}) ---")
    for s in sorted(mapped):
        print(f"  '{s}' -> {nsfrcd_fmt(s)}")
    
    print(f"\n--- UNMAPPED SET_IDs ({len(unmapped)}) ---")
    for s in sorted(unmapped):
        print(f"  '{s}'")
    
    return records
