# After od_ref_map is built
sample_accts = list(od_ref_map.keys())[:5]
for acct in sample_accts:
    od = od_ref_map[acct]
    print(f"DEBUG: ACCTNO={acct}, EXCESSDT={od['EXCESSDT']}, TODDATE={od['TODDATE']}")
