if out_rows:
    pl.DataFrame(out_rows, infer_schema_length=None).write_parquet(
        TRAN_DIR / "RENTAS_CIS.parquet")

if tran_inward_outward_rows:
    pl.DataFrame(tran_inward_outward_rows, infer_schema_length=None).write_parquet(
        TRAN_DIR / "INOUT.parquet")
