if row["FIRST_BRANCH"]:
    brhamt = brhac = 0
    if pagecnt > 0:
        out.append("\f")                     # <-- skip on first page
    pagecnt += 1
    out.extend(_header_a(...))
    linecnt = 5
